/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AppFunctional from './AppFunctional';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppFunctional);
